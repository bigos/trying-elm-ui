<h1>Struggling with Purescript</h1>

<p>After a day of struggle, I got to the point where I could work with the API
  and retrieve the data. Showing the list of data has proven harder than
  expected. Now I am stuck at function signatures where compiler gives me wrong
  sugestions.
</p>

<div id="halogen-cocktail">Please wait ...</div>

<%
js_filename = 'halogen-cocktail.js'
js_file = File.join(Rails.root, 'public', js_filename)
# prepare my own fingerprinting
js_mtime =File.mtime(js_file).to_i

html_attributes = {type: 'module', id: 'halogen-cocktail-flags'}
flag_data = {
  base_url: request.base_url,
  logname:  ENV['LOGNAME'],
  start: 7
}.each{ |k,v| html_attributes[:"data-#{k}"] = v }
%>

<%= javascript_include_tag "/#{js_filename}?#{js_mtime}", html_attributes %>
